"""
Tests Package for FAQ Finance Chatbot

This package contains all test modules for the FAQ Finance Chatbot service.
Tests are organized to match the application structure for easy maintenance.
"""

# Test configuration can be added here if needed
import sys
import os

# Add the parent directory to the Python path so tests can import the app
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))